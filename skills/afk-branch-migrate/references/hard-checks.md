# Hard Checks

- **No external APIs** — local Git operations only
- **Always create rollback checkpoint** before applying changes
- **High/Critical conflicts** require explicit user confirmation
- **No force-push or reset --hard** during migration
