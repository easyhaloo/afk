# api-auth-browser-session

This document has been superseded by
[browser-auth-modes.md](./browser-auth-modes.md), which defines the three
supported authentication reuse modes (`storage-state`, `persistent-profile`,
`localhost-cdp`) with selection rules, security requirements, and
generated-artifact contracts.

The original concept — "API login → reuse session in browser" — is now
covered by the `storage-state` mode with API setup.