# api-setup-browser-verify

API creates test data → Browser verifies UI display.

## Concept

Use API to quickly create test data, then navigate to a browser page to verify the data displays correctly in the UI.

## When to Use

- Create order via API → verify in browser
- Create user via API → verify profile page
- Setup test data → browser UI validation
