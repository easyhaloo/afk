# browser-action-api-verify

Browser performs action → API verifies side effects.

## Concept

User interacts with UI (clicks, forms, navigation), then verify the backend state changed correctly via API.

## When to Use

- User submits form → verify data created
- User clicks "Pay" → verify payment recorded
- User updates profile → verify changes persisted
